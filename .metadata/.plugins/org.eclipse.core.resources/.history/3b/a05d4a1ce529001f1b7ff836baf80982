<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Criar Consulta</title>
    <link rel="stylesheet" th:href="@{/css/bootstrap.min.css}">
</head>
<body>
    <div class="container">
        <h1 class="mt-4">Criar Consulta</h1>
        <form th:action="@{/consultas/new}" th:object="${consulta}" method="post">
            <div class="form-group">
                <label for="data">Data</label>
                <input type="date" id="data" th:field="*{data}" class="form-control" required/>
            </div>
            <div class="form-group">
                <label for="descricao">Descrição</label>
                <input type="text" id="descricao" th:field="*{descricao}" class="form-control" required/>
            </div>
            <div class="form-group">
                <label for="paciente">Paciente</label>
                <select id="paciente" th:field="*{paciente.id}" class="form-control" required>
                    <option th:each="paciente : ${pacientes}" th:value="${paciente.id}" th:text="${paciente.name}"></option>
                </select>
            </div>
            <div class="form-group">
                <label for="medico">Médico</label>
                <select id="medico" th:field="*{medico.id}" class="form-control" required>
                    <option th:each="medico : ${medicos}" th:value="${medico.id}" th:text="${medico.name}"></option>
                </select>
            </div>
            <button type="submit" class="btn btn-primary">Salvar</button>
        </form>
    </div>
    <script th:src="@{/js/bootstrap.bundle.min.js}"></script>
</body>
</html>
